import { createContext } from "react"

// valid / invalid / loading


export const UserContext = createContext(null as any)
export const UserValid = createContext(null as any)
export const UserLoad = createContext(null as any)
